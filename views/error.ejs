<%- include('partials/header') %>

<!-- Vue d'erreur générique -->
<div class="row">
    <div class="col-md-6 offset-md-3">
        <div class="card">
            <div class="card-body text-center">
                <h1 class="display-1">😕</h1>
                <h2><%= typeof title !== 'undefined' ? title : 'Erreur' %></h2>
                <p class="text-muted"><%= typeof message !== 'undefined' ? message : 'Une erreur est survenue' %></p>
                <a href="/products" class="btn btn-primary">Retour à l'accueil</a>
            </div>
        </div>
    </div>
</div>

<%- include('partials/footer') %>

